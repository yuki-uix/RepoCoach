{
  "name": "pnpm-mono",
  "version": "0.1.0",
  "private": true,
  "description": "Minimal pnpm workspace fixture for RepoCoach monorepo import (data only, never installed). Workspace members live in pnpm-workspace.yaml, not a workspaces field."
}
