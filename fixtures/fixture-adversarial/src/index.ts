// Conventional entry point so the adversarial repo still has a real surface for
// candidate generation / entry outlines to resolve against. The barrel hop to
// ./defs.js is deliberate: barrel penetration must resolve the real definition.
export * from "./defs.js";
