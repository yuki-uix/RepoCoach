/**
 * Agent Loop
 *
 * Self-implemented tool loop: assemble prompt, call the model, run read-only
 * repo tools, and parse the structured decision. See docs/architecture.md §3
 * for the module boundary. Not yet implemented — this is a module-boundary
 * placeholder.
 */
export {};
